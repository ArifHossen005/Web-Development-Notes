/* pre recorded 3
all data select 

-----  SELECT * FROM users;

-----  SELECT count(*), city FROM users GROUP BY city;  // kon city te koto customer  ache 
Note SELECT column_name FROM table_name GROUP BY column_name;
GROUP BY means to group the data by a column. 